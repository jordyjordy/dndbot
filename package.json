{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npx lerna run dev --parallel",
    "build": "npx lerna run build",
    "bootstrap": "npm install && lerna run bootstrap"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
