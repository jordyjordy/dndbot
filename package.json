{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npx lerna run dev --parallel",
    "build": "npx lerna run build",
    "bootstrap": "yarn install && lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0"
  }
}
